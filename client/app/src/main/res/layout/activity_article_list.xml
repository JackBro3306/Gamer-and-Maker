<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".ui.article.ArticleListActivity">
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/main_floating_action_btn"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="30dp"
        android:layout_marginBottom="40dp"
        app:backgroundTint="@color/floating_button"
        android:foregroundGravity="center"
        android:foreground="@drawable/ic_arrow_upward_white_24dp"
        app:layout_behavior="@string/fb_bottom_show_behavior" />
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">
        <include layout="@layout/view_article_list"/>
    </LinearLayout>


</androidx.coordinatorlayout.widget.CoordinatorLayout>